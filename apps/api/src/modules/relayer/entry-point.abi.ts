/**
 * The single EntryPoint v0.7 function this relayer calls — hand-written rather than
 * imported from contracts/out/ (that's a build artifact of a sibling package; the
 * running app shouldn't depend on it existing on disk). Matches
 * IEntryPoint.handleOps exactly; see contracts/lib/account-abstraction.
 */
export const ENTRY_POINT_ABI = [
  {
    type: 'function',
    name: 'handleOps',
    stateMutability: 'nonpayable',
    inputs: [
      {
        name: 'ops',
        type: 'tuple[]',
        components: [
          { name: 'sender', type: 'address' },
          { name: 'nonce', type: 'uint256' },
          { name: 'initCode', type: 'bytes' },
          { name: 'callData', type: 'bytes' },
          { name: 'accountGasLimits', type: 'bytes32' },
          { name: 'preVerificationGas', type: 'uint256' },
          { name: 'gasFees', type: 'bytes32' },
          { name: 'paymasterAndData', type: 'bytes' },
          { name: 'signature', type: 'bytes' },
        ],
      },
      { name: 'beneficiary', type: 'address' },
    ],
    outputs: [],
  },
] as const;
