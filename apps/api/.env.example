NODE_ENV="development"
# 3300, not the more common 3000, to avoid clashing with other local projects.
PORT=3300

# Populated in later phases: DATABASE_URL, REDIS_URL, SIGNER_BACKEND, chain RPC URLs, etc.
