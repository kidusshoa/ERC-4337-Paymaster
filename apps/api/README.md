# apps/api

NestJS Paymaster & Gas Relayer service. Scaffolded in Phase 5 of the build plan (see root README once written) — this directory is currently reserved so the monorepo layout is in place from the start.
