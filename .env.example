# Infra (docker-compose.yml) — copy to .env for local dev
# Ports are remapped (5434/6381) to avoid clashing with other local projects' default 5432/6379.
DATABASE_URL="postgresql://paymaster:paymaster@localhost:5434/paymaster?schema=public"
REDIS_URL="redis://localhost:6381"

# Populated in later phases (contracts, chain RPCs, signer config, etc.)
